{
  "name": "govuk-display-screen",
  "version": "1.0.0",
  "engines": {
    "node": "10.x",
    "npm": "6.x"
  },
  "scripts": {
    "build": "(cd eu-exit-screen && npm install install && npm run-script build && rm -rf ../public/eu-exit && mv ./build ../public/eu-exit && chmod 755 ../public/eu-exit)"
  }
}